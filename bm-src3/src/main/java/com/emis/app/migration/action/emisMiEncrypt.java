package com.emis.app.migration.action;/** * User: merlin * Date: Apr 25, 2003 * Time: 5:55:58 PM */public final class emisMiEncrypt extends emisMiAction {  public final String act(final String[] src, final String[] param) throws Exception {    if (src == null || src.length == 0 || src[0] == null)      return null;//        try {    final byte[] secret = encrypt(src[0]);    final sun.misc.BASE64Encoder encoder = new sun.misc.BASE64Encoder();    final String encoded = encoder.encode(secret);    return encoded;//        } catch (Exception e) {//            return src[0];//        }  }  static byte[] encrypt(final String x) throws Exception {    final java.security.MessageDigest d;    d = java.security.MessageDigest.getInstance("SHA-1");    d.reset();    d.update(x.getBytes());    return d.digest();  }}